# ════════════════════════════════════════════════════════════════════════════════
# Wiki Synchronization Environment Variables
# ════════════════════════════════════════════════════════════════════════════════
#
# IMPORTANT: This is a template file. Copy it to .env and fill in your actual values.
#
#   cp .env.example .env
#
# SECURITY: Never commit your .env file to version control. The .gitignore already
# excludes .env files to prevent accidental exposure of sensitive tokens.
#
# ════════════════════════════════════════════════════════════════════════════════

# ────────────────────────────────────────────────────────────────────────────────
# GitLab API Authentication
# ────────────────────────────────────────────────────────────────────────────────
#
# How to create a GitLab Personal Access Token:
#
# 1. Go to https://gitlab.slurpgg.net/-/user_settings/personal_access_tokens
# 2. Click "Add new token"
# 3. Name: "Bloom Website Wiki Sync"
# 4. Scopes: Check "read_api" (ONLY read_api, no write permissions needed)
# 5. Expiration: Set to your preference (recommend 1 year for stability)
# 6. Click "Create personal access token"
# 7. Copy the token immediately (you can't see it again after leaving the page)
# 8. Paste below replacing "your_token_here"
#
# Token format: glpat-xxxxxxxxxxxxxxxxxxxx
#
GITLAB_TOKEN=your_token_here

# ────────────────────────────────────────────────────────────────────────────────
# GitLab Project Configuration
# ────────────────────────────────────────────────────────────────────────────────
#
# Project ID for the Bloom game repository
#
# How to find your GitLab Project ID:
#
# 1. Go to https://gitlab.slurpgg.net/zachgonser/bloom
# 2. Look for "Project ID" under the project name in the sidebar
# 3. Alternatively, it's in the URL or Settings > General
#
# Example: If URL is gitlab.slurpgg.net/zachgonser/bloom, the project ID might be "123"
#
GITLAB_PROJECT_ID=your_project_id

# ────────────────────────────────────────────────────────────────────────────────
# Wiki Cache Configuration
# ────────────────────────────────────────────────────────────────────────────────
#
# Maximum age of cached wiki files before refetching (in milliseconds)
#
# Default: 86400000 ms = 24 hours
#
# Cache behavior:
# - If temp/wiki-raw/*.md files are NEWER than this age: Use cached files (fast)
# - If temp/wiki-raw/*.md files are OLDER than this age: Refetch from GitLab (slower)
# - To force refetch regardless of cache age: Use --force flag
#
# Recommended values:
# - Development: 86400000 (24 hours) - refetch once per day
# - CI/CD: 3600000 (1 hour) - ensure fresh data on builds
# - Testing: 0 (always refetch) - useful for debugging wiki changes
#
WIKI_CACHE_MAX_AGE=86400000

# ────────────────────────────────────────────────────────────────────────────────
# Optional: GitLab Instance URL
# ────────────────────────────────────────────────────────────────────────────────
#
# Only needed if using a self-hosted GitLab instance (not GitLab.com)
# Default: https://gitlab.slurpgg.net
#
# Uncomment and modify if your GitLab is at a different URL:
# GITLAB_HOST=https://gitlab.slurpgg.net
